#include <iostream>
using namespace std;

int main() {
    int n = 4;

    long long d[5];
    d[0] = 1;
    d[1] = 0;

    for (int i = 2; i <= n; i++) {
        d[i] = (i - 1) * (d[i - 1] + d[i - 2]);
    }

    cout << d[n] << endl;
    return 0;
}
