#include <bits/stdc++.h>
using namespace std;

int main() {
    int N = 5, M = 5;

    int grid[5][5] = {
        { 1,  5, -1,  1,  0},
        { 4,  0,  3,  0,  2},
        { 3,  2,  6,  2, -10},
        { 2,  0, -10, 4,  1},
        { 3,  4,  5,  0,  7}
    };

    int answer = INT_MIN;

    for (int top = 0; top < N; top++) {
        vector<int> temp(M, 0);

        for (int bottom = top; bottom < N; bottom++) {
            for (int col = 0; col < M; col++) {
                temp[col] += grid[bottom][col];
            }

            int cur = 0;
            for (int col = 0; col < M; col++) {
                cur = max(temp[col], cur + temp[col]);
                answer = max(answer, cur);
            }
        }
    }

    cout << answer << "\n";  // 34
    return 0;
}
